export const StoneWrap = (props) => {
    const{children} = props
    return(
        <div className="stoneWrap">
            {children}
        </div>
    )
}
export const StoneMedia = (props) => {
    const{children} = props
    return(
        <div className="stoneMedia">
            {children}
        </div>
    )
}
export const StoneInfo = (props) => {
    const{children} = props
    return(
        <div className="stoneInfo">
            {children}
        </div>
    )
}